
pipeline {

  agent { label 'alpine_controller' }
  
  options { skipDefaultCheckout(true) }
  
  stages {

    stage('clean workspace') { 
      steps {
        cleanWs()
      }
    }

    stage('checkout') {
      steps {
        checkout scm
      }
    }    

    stage('hello') {
      steps {
        sh 'ansible-galaxy collection install -r requirements.yml'
        sh 'ansible-playbook -i inventory/hosts playbooks/hello.yml'
      }
    }

  }

  post { always { cleanWs() } }

}